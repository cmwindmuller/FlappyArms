using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// for moving everything(textures,particles mostly) Never moves the player (endless-running style)
/// </summary>
public class World : MonoBehaviour {
    public static Vector3 movement;
}
